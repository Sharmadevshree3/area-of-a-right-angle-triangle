# area-of-a-right-angle-triangle
#include <stdio.h>

int main()
{
    float base, height, area;
   printf("enter the value of base: ");
   scanf("%f", &base);
   printf("enter the height: ");
   scanf ("%f", &height);
   printf("area is %f", area=base*height*0.5);
    return 0;
}
